This code creates a executable program that can be used as a shell in Unix. This behaves similar to bash with several changes.
It will allow foreground and background processes just like a typical shell.
It will also clean up processes after they finish executing.
